providers = ["python"]

[variables]
PYTHONPATH = ".:/app:/app/backend:.."

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "python -m uvicorn main:app --host 0.0.0.0 --port $PORT --app-dir ."
